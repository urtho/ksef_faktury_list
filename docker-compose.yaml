services:
  ksef:
    build: .
    volumes:
      - ./config.json:/app/config.json:ro
      - ./certs:/certs:ro
      - ./output:/output
      - ./meta:/app/meta
      - ./state.json:/app/state.json
    command:
      - -c
      - /app/config.json
