services:
  ksef:
    build: .
    volumes:
      - ./config.json:/app/config.json:ro
      - ./certs:/certs:ro
      - ./output:/output
      - ./meta:/app/meta
    command:
      - -c
      - /app/config.json
